<%
for(attr in attrs){
if(!isBaseField(attr.colName)){
%>
	<%if(!isEmpty(attr.comment)){%>
	/**
	* ${attr.comment}
	*/
	@ApiModelProperty(value="${attr.comment}",notes="长度为：${attr.colSize}")
	<%}%>
	private ${attr.javaType} ${attr.name};
<%
}}
%>