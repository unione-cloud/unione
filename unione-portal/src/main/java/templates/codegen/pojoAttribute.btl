<%
for(attr in attrs){
if(!isBaseField(attr.colName)){
%>
	<%if(!isEmpty(attr.comment)){%>
	/**
	* ${attr.comment}
	*/
	@Schema(title="${attr.comment}",description="长度为：${attr.colSize}")
	<%}%>
	private ${attr.javaType} ${attr.name};
<%
}}
%>